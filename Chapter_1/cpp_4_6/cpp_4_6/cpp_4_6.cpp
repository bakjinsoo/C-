#include<iostream>
#include<limits>
using namespace std;

int main()
{
	int num = 0;
	num == 0 ? (cout << "num==0") : (cout << "num!=0");//삼항 연산자 num이 0인게 true면 왼쪽 문을 실행, false면 오른쪽문 실행
	//size of 연산자
	// :: 연산자 지역변수 전역변수이름이 같을때 ::num을 해줘야 전역변수로 접근
	
	//형변환 int('a') (int)'a'명시적 형변환
	
}